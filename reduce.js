// reduce method reduces the value to a ingle value

// syntax :
// arr.reduce(acumulator ,element);

// [1,2,3,4].reduce((res,el)=>(res +el));  //10

let num =[1,2,3,4,5,6,6];
let result =num.reduce(res,el)=>(re+el);
console.log(result); // 10